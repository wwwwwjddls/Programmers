https://programmers.co.kr/learn/courses/30/lessons/12937

[풀이]
1. 2로나누어 나머지가 0이면 Even, 아니면 Odd를 answer에 담는다.

[소스 - JavaScript]
function solution(num) {
  var answer = '';
  return answer = (num%2 === 0) ? 'Even' : 'Odd';
}

[소스 - Java]
class Solution {
  public String solution(int num) {
    String answer = "";
    return answer = (num%2 == 0) ? "Even" : "Odd";
    }
}
